using UnityEngine;
using System.Collections;

public class MapInfo : MonoBehaviour {
	public enum Affiliation{none,red,blue};
	public static Color red = new Color(255,0,150,255);
	public static Color blue = new Color(0,255,255,255);
	public static Color white = Color.white;
	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
